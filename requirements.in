# Add your integration-specific dependencies here
earthranger-client>=1.15.0,<2.0.0
# backports.zoneinfo dropped — repo is permanently on Python 3.10+ (see
# Dockerfile + .python-version), so the zoneinfo stdlib module is always
# available. Keeping the entry caused Docker builds to fail because the
# marker was being stripped during uv pip compile and the 0.2.1 C extension
# uses _PyLong_One which Python 3.10 removed.
# respx is imported by app/actions/tests/test_er_schema.py; gundi-client-v2 2.4 used
# to pull it in transitively, 3.x does not. 0.22 is the first release for httpx 0.28.
respx~=0.22.0
